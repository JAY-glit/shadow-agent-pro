# Testing and dev tooling — not needed to run the app itself, only to
# develop/test it. Install with: pip install -r requirements-dev.txt
-r requirements.txt

pytest==8.3.3
pytest-cov==5.0.0
flake8==7.1.1
